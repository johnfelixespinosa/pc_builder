<% if user_signed_in? %>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/">PC-Builder!</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar6">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="navbar-collapse collapse justify-content-stretch" id="navbar6">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <%= link_to "My Parts Lists", user_parts_lists_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "My Builds", user_builds_path(current_user), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Explore Builds", builds_path(search: "all"), class: "nav-link" %>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
              <%= notice %>
          </li>
          <li class="nav-item">
              <%= alert %>
          </li>
          <li class="nav-item">
            <%= link_to "Logged in as: #{current_user.username.titleize}", current_user, class: "nav-link" %></li>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign out', destroy_user_session_path, :method => :delete, class: "nav-link" %>
          </li>
        </ul>
      </div>
  </nav>

<% else %>

  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <a class="navbar-brand" href="/">PC-Builder!</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar6">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="navbar-collapse collapse justify-content-stretch" id="navbar6">
        <ul class="navbar-nav">
          <li class="nav-item">
            <%= link_to "Explore Builds", builds_path, class: "nav-link" %>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <%= notice %>
          </li>
          <li class="nav-item">
            <%= alert %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %></li>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign In', new_user_session_path, class: "nav-link" %>
          </li>
        </ul>
      </div>
  </nav>

<% end %>